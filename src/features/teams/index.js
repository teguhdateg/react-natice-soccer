import React, {Component} from 'react';
import {View, Text} from 'react-native';

class TeamsScreen extends Component {
    render() {
        return (
            <View style={{
                flex: 1,
                justifyContent: 'center',
                alignItems: 'center'
            }}>
                <Text>Screen Team</Text>
            </View>
        )
    }
}

export default TeamsScreen;